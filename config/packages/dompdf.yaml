# DomPDF Configuration for Invoice Generation
parameters:
    dompdf.options:
        # Paper size
        defaultPaperSize: 'A4'
        defaultPaperOrientation: 'portrait'

        # Font settings
        defaultFont: 'DejaVu Sans'
        isFontSubsettingEnabled: true

        # SECURITY: Remote content disabled to prevent SSRF.
        # Logos must be local file paths, not URLs.
        isRemoteEnabled: false

        # Enable HTML5 parser
        isHtml5ParserEnabled: true

        # Cache and temp directories
        fontDir: '%kernel.project_dir%/var/fonts'
        fontCache: '%kernel.project_dir%/var/fonts'
        tempDir: '%kernel.project_dir%/var/tmp'

        # Debug
        debugCss: false
        debugLayout: false
        debugLayoutLines: false
        debugLayoutBlocks: false
        debugLayoutInline: false
        debugLayoutPaddingBox: false
